---
- name: Installer apache2 et configure la page d'acceuil
  hosts: all
  remote_user: vagrant
  become: true
  become_user: root

  tasks:
    - name: Install apache2
      ansible.builtin.apt:
        update_cache: true
        name: apache2
        state: present
